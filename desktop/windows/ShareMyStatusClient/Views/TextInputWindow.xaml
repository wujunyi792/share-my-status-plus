<Window x:Class="ShareMyStatusClient.Views.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入"
        Icon="pack://application:,,,/Resources/app.ico"
        Width="520" Height="420"
        MinWidth="380" MinHeight="280"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI, Microsoft YaHei">
    <DockPanel Margin="12">
        <Border DockPanel.Dock="Bottom" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="确定" Width="90" Margin="0,0,8,0" IsDefault="True" Click="OnOkClick"/>
                <Button Content="取消" Width="90" IsCancel="True" Click="OnCancelClick"/>
            </StackPanel>
        </Border>

        <TextBlock x:Name="LblHint" DockPanel.Dock="Top" Margin="0,0,0,8"
                   Foreground="#666" TextWrapping="Wrap"/>

        <TextBox x:Name="Txt" AcceptsReturn="True" AcceptsTab="False"
                 TextWrapping="NoWrap" VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas, Cascadia Mono"/>
    </DockPanel>
</Window>
